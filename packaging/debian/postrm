#!/bin/sh
set -e

# Restart a running fprintd so it drops the removed module.
if [ "$1" = "remove" ] && [ -d /run/systemd/system ]; then
    systemctl try-restart fprintd.service >/dev/null 2>&1 || true
fi

#DEBHELPER#

exit 0
