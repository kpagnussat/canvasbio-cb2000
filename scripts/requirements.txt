# CB2000 scripts dependencies
#
# This file belongs to the scripts/tooling flow, not to the driver core.
# The currently published helpers use only the Python standard library, so
# there are no mandatory third-party Python wheels at this snapshot.
#
# Keep this file here so future script-side dependencies can be declared in one
# place without changing the basic venv/bootstrap flow.
